{{/* Worldbuilding topic picker — the /wbtopic command. Reads the single DB entry
     seeded by wb_seeder.txt (key "wbquestions", owner 0) — SEPARATE from the
     "topic", "rpquestions", and "ocquestions" keys, so the lists never mix.
     One DB call. */}}
{{ $q := (dbGet 0 "wbquestions").Value }}
{{ if $q }}{{ index $q (randInt (len $q)) }}{{ else }}No worldbuilding topics loaded yet.{{ end }}
